package;

import TestMacros.*;
import RangeTools.*;

class MacroTesting {
    public static function main() {
        // for (index in 2...15) {
        //     trace(range(0, index));
        // }
        
        // trace(zip(range(0, 5), [4, 5, 6, 7, 8, 9], range(9, 14)));

      printSomething(123);
    }
}
